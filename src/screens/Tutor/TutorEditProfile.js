import React from 'react';

import Header from '../../components/Header';

export default function Message({navigation}: any){
    return(
        <Header 
            headerTitle={"Edit Profile"}
        />
    );
}